# Абсолютно минимальный Dockerfile
FROM alpine:latest

# Не пытаемся ничего копировать
RUN echo "Creating main service container"

WORKDIR /app

# Просто запускаем бесконечный sleep
CMD echo "Main Service is running" && sleep infinity